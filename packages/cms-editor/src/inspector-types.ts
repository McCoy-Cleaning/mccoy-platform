import type { CmsImagePickerProps } from "./image-picker-props";

/** @deprecated Prefer `CmsImagePickerProps` — kept as alias for inspector props. */
export type ImagePickerProps = CmsImagePickerProps;
