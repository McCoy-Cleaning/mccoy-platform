# Self-hosted Archivo (latin + latin-ext)

Variable-font subsets from Google Fonts (`Archivo` v25), used only for `.font-display`.
Body copy stays on the system Helvetica stack — do not add more weights without measuring LCP.
