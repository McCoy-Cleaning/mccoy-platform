/**
 * Re-export shared domain types. Prefer importing from `@mccoy/domain` in new code.
 */
export {
  FORM_KINDS,
  FORM_SUBJECTS,
  type FormKind,
  type FormAttachment,
  type WebsiteFormPayload,
} from "@mccoy/domain";
